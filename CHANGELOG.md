## 0.0.5

- Refactored `Responsive` from a static class to a `BuildContext` extension for cleaner API usage.
- Replaced `MediaQuery.of(context).size` with `MediaQuery.sizeOf(context)` for better performance.

## 0.0.4

- Initial release: basic landscape video playback with native aspect ratio handling.

